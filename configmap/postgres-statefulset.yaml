# PostgreSQL StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-db
spec:
  serviceName: postgres-db-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres-db
  template:
    metadata:
      labels:
        app: postgres-db
    spec:
      containers:
      - name: postgres-container
        image: postgres:latest
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        - name: init-sql
          mountPath: /docker-entrypoint-initdb.d/
        env:
        - name: POSTGRES_PASSWORD
          value: testpassword
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
      volumes:
        - name: init-sql
          configMap:
            name: init-sql
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
        
# Helpful commands for debugging
# kubectl scale statefulset postgres-db --replicas=0 && kubectl delete statefulset postgres-db && kubectl delete pvc data-postgres-db-0
# kubectl create configmap init-sql --from-file=./statefulset/init.sql --save-config
# kubectl create configmap init-sql --from-file=./statefulset/init.sql --dry-run=client -o yaml | kubectl apply -f -
# kubectl apply -f statefulset/postgres-statefulset.yaml && kubectl get pods -A
# kubectl exec -it postgres-db-0 -- /bin/bash